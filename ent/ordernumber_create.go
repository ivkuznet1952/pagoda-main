// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/mikestefanello/pagoda/ent/ordernumber"
)

// OrderNumberCreate is the builder for creating a OrderNumber entity.
type OrderNumberCreate struct {
	config
	mutation *OrderNumberMutation
	hooks    []Hook
}

// SetNum sets the "num" field.
func (_c *OrderNumberCreate) SetNum(v int) *OrderNumberCreate {
	_c.mutation.SetNum(v)
	return _c
}

// SetNillableNum sets the "num" field if the given value is not nil.
func (_c *OrderNumberCreate) SetNillableNum(v *int) *OrderNumberCreate {
	if v != nil {
		_c.SetNum(*v)
	}
	return _c
}

// Mutation returns the OrderNumberMutation object of the builder.
func (_c *OrderNumberCreate) Mutation() *OrderNumberMutation {
	return _c.mutation
}

// Save creates the OrderNumber in the database.
func (_c *OrderNumberCreate) Save(ctx context.Context) (*OrderNumber, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *OrderNumberCreate) SaveX(ctx context.Context) *OrderNumber {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *OrderNumberCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *OrderNumberCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *OrderNumberCreate) defaults() {
	if _, ok := _c.mutation.Num(); !ok {
		v := ordernumber.DefaultNum
		_c.mutation.SetNum(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *OrderNumberCreate) check() error {
	if _, ok := _c.mutation.Num(); !ok {
		return &ValidationError{Name: "num", err: errors.New(`ent: missing required field "OrderNumber.num"`)}
	}
	return nil
}

func (_c *OrderNumberCreate) sqlSave(ctx context.Context) (*OrderNumber, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *OrderNumberCreate) createSpec() (*OrderNumber, *sqlgraph.CreateSpec) {
	var (
		_node = &OrderNumber{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(ordernumber.Table, sqlgraph.NewFieldSpec(ordernumber.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.Num(); ok {
		_spec.SetField(ordernumber.FieldNum, field.TypeInt, value)
		_node.Num = value
	}
	return _node, _spec
}

// OrderNumberCreateBulk is the builder for creating many OrderNumber entities in bulk.
type OrderNumberCreateBulk struct {
	config
	err      error
	builders []*OrderNumberCreate
}

// Save creates the OrderNumber entities in the database.
func (_c *OrderNumberCreateBulk) Save(ctx context.Context) ([]*OrderNumber, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*OrderNumber, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*OrderNumberMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *OrderNumberCreateBulk) SaveX(ctx context.Context) []*OrderNumber {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *OrderNumberCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *OrderNumberCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
